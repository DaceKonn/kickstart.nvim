-- A plugin to help display code context

return {
  'SmiteshP/nvim-navic',
  dependencies = { 'nvim-tree/nvim-web-devicons' }, -- Optional: for icons
  opts = {
    -- Options for nvim-navic (see nvim-navic's documentation for more)
    -- e.g., icons = {}, highlight = true, depth_limit = 0,
  },
}
